// distancia 
#include <stdio.h>

int main(){
    int X = 60, Y = 90, distancia;

    scanf("%d", &distancia);

    distancia = distancia * 2;

    printf("%d minutos\n", distancia);

    return 0;
}